from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='We are creating a covid -19 Data analysis prototype for better visualization of data.',
    author='Kannan',
    license='',
)
